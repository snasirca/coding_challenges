require "coding_challenges/<%= ENV["EXERCISE_KEY"] %>"

RSpec.describe "Solution" do
  it "1" do
    nums = [1, 2, 3]

    result = <%= ENV["EXERCISE_MAIN_METHOD"] %>

    expect(result).to eq(true)
  end
end
