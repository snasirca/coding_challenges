require "coding_challenges/<%= ENV["EXERCISE_KEY"] %>"

RSpec.describe CodingChallenges::<%= ENV["EXERCISE_CLASS_NAME"] %> do
  subject { CodingChallenges::<%= ENV["EXERCISE_CLASS_NAME"] %>.new }

  it "1" do
    root = [1, 2, 3]

    result = subject.<%= ENV["EXERCISE_MAIN_METHOD"] %>

    expect(result).to eq(true)
  end
end
